name: Hello Manual Run

on:
  workflow_dispatch:  # 수동 실행 트리거

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello, GitHub Actions! (manual run)"
